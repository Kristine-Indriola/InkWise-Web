// Placeholder global bundle for InkWise legacy views.
// TODO: migrate features into modular entry points or remove if unused.

if (window?.console) {
  console.debug('[InkWise] app.js bundle loaded');
}
